{
  "name": "pi-journal",
  "version": "1.0.0",
  "description": "Session journaling extension for pi-coding-agent",
  "main": "index.ts",
  "dependencies": {
    "@pi-atelier/shared-utils": "github:catlain/pi-shared-utils",
    "pi-session-analyzer": "github:catlain/pi-session-analyzer"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=1.0.0",
    "@earendil-works/pi-tui": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    },
    "@earendil-works/pi-tui": {
      "optional": true
    }
  },
  "pi": {
    "extensions": [
      "."
    ]
  },
  "files": [
    "index.ts",
    "lib/",
    "README.md",
    "README.en.md",
    "tsconfig.json",
    "package.json"
  ]
}
